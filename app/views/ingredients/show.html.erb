   <h3>Ingredients Page</h3>

   <div><%= link_to(@ingredient.name, @ingredient) %></div>
   <div><%= @ingredient.measurement %></div>
   <div><%= @ingredient.cost %></div>
   <div><%= image_tag @ingredient.image, :class => "indeximage" %></div>

      <br><Br>


   <%= @book %>
   <%= image_tag @book.image, :class => 'bookimages' %>

   <%= link_to 'Delete', @ingredient, :method => :delete, :confirm => 'Are you sure?'  %>
   <%= link_to 'Edit', edit_ingredient_path(@ingredient)  %>
